# This single line is the whole of what arxdeus_lints asks of a project.
include: package:arxdeus_lints/analysis_options/recommended.yaml

# Exclude paths are per-project, so the shared file sets none. Set yours here.
analyzer:
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"
