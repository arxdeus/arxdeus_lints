name: arxdeus_lints
description: >-
  The shared analysis options behind the disposito, checked_exceptions and
  sensitive_exposure_lint analyzer plugins. Include the options from your own
  analysis_options.yaml to adopt the same configuration.
version: 2.0.0
repository: https://github.com/arxdeus/arxdeus_lints
issue_tracker: https://github.com/arxdeus/arxdeus_lints/issues

environment:
  sdk: ^3.13.2

resolution: workspace
